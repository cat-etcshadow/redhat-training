## Configure umask

Create user **{{UMASK_USER}}** if it does not already exist, then set a persistent umask of **{{UMASK_VAL}}** for **{{UMASK_USER}}** only, and set the same umask **{{UMASK_VAL}}** system-wide for all users.
