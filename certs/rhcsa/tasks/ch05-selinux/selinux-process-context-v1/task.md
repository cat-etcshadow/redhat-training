## Identify SELinux process context of running services

The **httpd** service is installed and running, and SELinux must remain in **Enforcing** mode. Write the SELinux **type** of the running **httpd** process, and nothing else, to **{{OUTPUT_FILE}}**, then append the SELinux **type** of the running **sshd** process on a second line.
