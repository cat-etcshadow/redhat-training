## Discover and mount an undocumented NFS export

An NFS server is running on this system (**localhost**), exporting a share, but its exact export path is not documented. Discover the available exports on **localhost** and save the output to **{{REPORT_FILE}}**, then mount the discovered export at **{{MOUNT_POINT}}** as a one-time mount that does not need to be persistent.
