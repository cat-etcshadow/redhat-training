## Write a script using functions

Create an executable script at **{{SCRIPT_PATH}}** that generates a disk usage report, defining and using functions — including one that prints a report header with the current date, and one that checks a single mount point, reporting whether it is mounted and its usage if so. The script must check `/` and `/boot`, reporting each mount point's status as OK or WARN.
