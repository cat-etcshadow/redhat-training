## Extend a logical volume and resize the filesystem

A logical volume **{{LV_NAME}}** in volume group **{{VG_NAME}}** is already provisioned and mounted at **{{MOUNT_POINT}}** with an XFS filesystem, and the application needs more space. Extend **{{LV_NAME}}** by **{{EXTEND_BY}}** and grow the XFS filesystem on **{{MOUNT_POINT}}** to use the new space online.
