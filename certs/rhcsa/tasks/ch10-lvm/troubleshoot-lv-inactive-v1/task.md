## Restore access to database storage after maintenance

The logical volume that provides storage for /mnt/dbdata was working normally before a recent maintenance window, but the data is no longer accessible there. Restore access so that /mnt/dbdata is mounted and available, and confirm the configuration will still work after a reboot.
