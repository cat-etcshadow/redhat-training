## Extend a logical volume and resize the ext4 filesystem

A logical volume **{{LV_NAME}}** in volume group **{{VG_NAME}}** is already provisioned and mounted at **{{MOUNT_POINT}}** with an **ext4** filesystem. Extend **{{LV_NAME}}** by **{{EXTEND_BY}}** and grow the ext4 filesystem to fill the new space.
