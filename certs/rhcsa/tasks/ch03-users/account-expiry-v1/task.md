## Set an account expiration date for a temporary user

The temporary user **{{CONTRACT_USER}}** already exists on the system.

Your task:

Configure **{{CONTRACT_USER}}'s** account to expire on **{{EXPIRE_DATE}}**,
so the account becomes unusable after that date.
