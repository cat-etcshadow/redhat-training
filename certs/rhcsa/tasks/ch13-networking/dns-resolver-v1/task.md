## Configure DNS resolvers with nmcli

Configure the system's primary Ethernet connection to use **{{DNS1}}** and **{{DNS2}}** as DNS servers, in that priority order, ignoring any DNS servers provided by DHCP, so that `/etc/resolv.conf` reflects the new resolvers.
