## Configure DNS resolvers with nmcli

Your task:

1. Configure the system's primary Ethernet connection to use
   **{{DNS1}}** and **{{DNS2}}** as DNS servers, in that priority order.

2. Ensure DNS servers provided by DHCP are **ignored**.

3. Apply the change and verify `/etc/resolv.conf` reflects the new
   resolvers.
