## Add a secondary IPv4 address to an existing connection

Add **{{SECONDARY_IP}}** as an additional IPv4 address on the active NetworkManager connection on the primary network interface, without removing its existing address — the interface must end up with both its original address and **{{SECONDARY_IP}}**.
