## Configure a static IP address with nmcli

The system's primary NIC currently uses DHCP. Configure it with a static IPv4 address of **10.0.0.50/24**, gateway **10.0.0.1**, DNS servers **8.8.8.8** and **1.1.1.1**, set to autoconnect on boot.
