## Create a new NetworkManager connection profile with nmcli

Create a new NetworkManager connection profile named **{{CON_NAME}}**, of type `dummy`, for interface **{{IFACE_NAME}}**, with a static IPv4 address of **{{IP_ADDR}}** (manual method), set to not auto-connect at boot, and brought up so the address is applied to **{{IFACE_NAME}}**.
