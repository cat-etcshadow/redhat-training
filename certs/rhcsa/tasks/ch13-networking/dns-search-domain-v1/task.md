## Configure a DNS search domain with nmcli

Your task:

1. Configure the system's primary Ethernet connection to use
   **{{SEARCH_DOMAIN}}** as a DNS search domain.
2. Apply the change and verify `/etc/resolv.conf` reflects the new
   search domain.
