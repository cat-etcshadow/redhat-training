## Configure a DNS search domain with nmcli

Configure the system's primary Ethernet connection to use **{{SEARCH_DOMAIN}}** as a DNS search domain, so that `/etc/resolv.conf` reflects the new search domain.
