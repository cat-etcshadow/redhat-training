## Configure a persistent static route

Add a persistent static route for **{{STATIC_NET}}** via gateway **{{STATIC_GW}}** to the primary network connection using NetworkManager, applied so the route is active in the current routing table.
