## Configure a persistent static route

Your task:

1. Identify the name of the primary network connection.
2. Add a persistent static route for **{{STATIC_NET}}** via gateway **{{STATIC_GW}}**
   to that connection using NetworkManager.
3. Bring the connection up to apply the changes.
4. Verify the route appears in the routing table.
