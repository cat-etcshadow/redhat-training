## Restore SSH key-based login for an administrator

The user **sshadmin** previously could connect to this system over SSH using key-based authentication, but attempts to do so now fail. Restore sshadmin's ability to log in over SSH using their existing key pair.
