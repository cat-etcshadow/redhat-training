## Create a custom rsyslog rule to filter log messages

Create a custom rsyslog configuration file at `{{CONF_FILE}}` that captures all **{{FACILITY}}** facility messages at **{{SEVERITY}}** severity or higher and writes them to **{{LOG_FILE}}**, with rsyslog running with this configuration applied.
