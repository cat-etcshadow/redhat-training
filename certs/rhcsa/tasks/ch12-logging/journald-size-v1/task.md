## Configure journald persistent storage and max size

Configure journald to store logs persistently on disk, cap the total journal disk usage at **{{MAX_USE}}**, and restart the journald service to apply the changes.
