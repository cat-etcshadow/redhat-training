## Configure journald for persistent log storage

Configure journald to store logs persistently on disk with a maximum journal disk usage of **100M**, and restart the `systemd-journald` service to apply the changes.
