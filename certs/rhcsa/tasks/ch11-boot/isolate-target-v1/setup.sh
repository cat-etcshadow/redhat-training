#!/usr/bin/env bash
# No state to seed — this is a procedural task (isolate/return, verified via
# journalctl + current runtime/default target), not a "fix the broken thing"
# task, so there's nothing to break beforehand.
true
