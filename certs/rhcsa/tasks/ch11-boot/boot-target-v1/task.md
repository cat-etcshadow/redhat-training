## Change the default systemd boot target

The system is currently set to boot into graphical mode. Set the default boot target to **multi-user.target** persistently, so the change survives a reboot.
