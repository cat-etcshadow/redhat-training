## Change the default systemd boot target

The system is currently set to boot into graphical mode.

Your task:

1. Set the default boot target to **multi-user.target** persistently.
2. The change must survive a reboot.
