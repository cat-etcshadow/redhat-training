## Reset the root password via emergency boot

The root password has been changed and is now unknown.

Recover access and set the root password to **RedHat123!**. The system must
boot normally afterward.
