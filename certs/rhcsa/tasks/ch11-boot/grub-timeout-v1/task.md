## Set the GRUB boot menu timeout persistently

Set the GRUB boot menu timeout to **{{TIMEOUT_VAL}}** seconds in `/etc/default/grub`, and regenerate the boot loader configuration so the change takes effect on the next boot.
