## Set the GRUB boot menu timeout persistently

Your task:

1. Set the GRUB boot menu timeout to **{{TIMEOUT_VAL}}** seconds in
   `/etc/default/grub`.

2. Regenerate the boot loader configuration so the change takes effect on
   the next boot.
