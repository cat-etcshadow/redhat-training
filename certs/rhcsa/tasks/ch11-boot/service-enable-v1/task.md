## Enable a service to start at boot and configure it

Your task:

1. Install the **postfix** service if not already installed.
2. Enable **postfix** to start automatically at boot.
3. Start the **postfix** service immediately.
