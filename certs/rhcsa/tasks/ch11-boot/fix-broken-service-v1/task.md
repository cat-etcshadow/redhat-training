## Diagnose and fix a broken systemd unit file

The service **rhtr-brokensvc.service** is enabled but fails to start due to a misconfigured unit file. Fix the unit file so the service starts successfully and is both active and enabled.
