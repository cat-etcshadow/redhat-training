## Diagnose and fix a broken systemd unit file

The service **rhtr-brokensvc.service** is enabled but fails to start due
to a misconfigured unit file.

Your task:

1. Diagnose why **rhtr-brokensvc.service** is failing to start.
2. Fix the unit file so the service starts successfully.
3. The service must be **active** and **enabled**.
