## Remove a kernel parameter via grubby

The default kernel entry currently has the kernel parameter
**{{KERNEL_PARAM}}** set.

Your task:

Remove the kernel parameter **{{KERNEL_PARAM}}** from the default kernel
entry.
