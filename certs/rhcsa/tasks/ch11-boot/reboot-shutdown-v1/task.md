## Schedule and Cancel a System Shutdown

Schedule the system to reboot in 10 minutes using the `shutdown` command, with the broadcast message `Maintenance restart`, then cancel the scheduled reboot before it takes effect. When you are finished, the system must have no pending shutdown or reboot scheduled, and must not have actually rebooted.
