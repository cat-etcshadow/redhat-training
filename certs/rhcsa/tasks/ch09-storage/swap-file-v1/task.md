## Create and activate a swap file

Your task:

1. Create a swap file at **{{SWAP_FILE}}** of size **{{SWAP_SIZE_MB}}MB**.
2. Set its permissions to **600** (owner read/write only).
3. Format it as swap and activate it immediately.
4. Make it persistent across reboots via `/etc/fstab`.
