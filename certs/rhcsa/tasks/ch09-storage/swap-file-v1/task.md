## Create and activate a swap file

Create a swap file at **{{SWAP_FILE}}** of size **{{SWAP_SIZE_MB}}MB**, with permissions set to **600** (owner read/write only). Format it as swap, activate it immediately, and make it persistent across reboots via `/etc/fstab`.
