## Create XFS partition and mount persistently

An additional **{{DISK_SIZE}}** block device is attached to the system. Create a single **{{PART_SIZE}}** primary partition on it, format it as **XFS**, and mount it persistently at **{{MOUNT_POINT}}** using its UUID in `/etc/fstab`.
