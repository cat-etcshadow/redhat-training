## Restore access to the application data filesystem

The directory /mnt/appdata is supposed to hold application data on a dedicated filesystem, but the data is not accessible there after a reboot. Correct the problem so that /mnt/appdata is mounted automatically at boot and the configuration survives a reboot.
