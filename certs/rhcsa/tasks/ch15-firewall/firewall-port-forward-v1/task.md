## Configure a permanent firewalld port forward

Your task:

Configure a **permanent** port forward in the **public** zone so that
traffic arriving on port **{{FROM_PORT}}/tcp** is redirected to local port
**{{TO_PORT}}**.
