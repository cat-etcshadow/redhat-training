## Allow HTTP and HTTPS traffic through firewalld

Your task:

1. Ensure **firewalld** is running and enabled at boot.
2. Add the **http** and **https** services to the **public** zone permanently.
3. Reload firewalld and verify with `firewall-cmd --list-all`.
