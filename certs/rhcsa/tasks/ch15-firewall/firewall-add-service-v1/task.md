## Allow HTTP and HTTPS traffic through firewalld

Ensure **firewalld** is running and enabled at boot, permanently add the **http** and **https** services to the **public** zone, and reload firewalld to apply the changes.
