## Permanently remove a service from a firewalld zone

The service **{{SVC_NAME}}** is currently permanently enabled in the
**public** zone, but it should not be exposed.

Your task:

Permanently remove **{{SVC_NAME}}** from the public zone.
