## Use find to locate files

Search **{{SEARCH_DIR}}** recursively for all regular files matching the pattern **{{PATTERN}}**, and write the full path of each match, one per line and sorted alphabetically, to **{{OUTPUT_FILE}}**.
