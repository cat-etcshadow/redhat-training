## Create and extract a compressed tar archive

Create a **gzip-compressed** tar archive of **{{SRC_DIR}}** and save it as **{{ARCHIVE_DIR}}/{{ARCHIVE_NAME}}**, creating **{{ARCHIVE_DIR}}** if it does not exist. Extract the archive into **{{EXTRACT_DIR}}** (creating it if needed) so that its contents match **{{SRC_DIR}}** with the original directory structure preserved.
