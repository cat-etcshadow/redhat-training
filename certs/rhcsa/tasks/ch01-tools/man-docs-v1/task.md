## Locate and use system documentation

The exam environment has no internet access; rely on `man` pages and on-system documentation to find the options you need. Copy `/etc/hosts` to **{{DEST_FILE}}**, preserving its permissions, timestamps, ownership, and symbolic links, and write a list of all files in `/var/log` larger than **1 MB** to **{{OUTPUT_FILE}}**.
