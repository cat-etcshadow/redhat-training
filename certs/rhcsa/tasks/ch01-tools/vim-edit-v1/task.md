## Edit a configuration file with a text editor

The file `{{CONF_FILE}}` contains placeholder values that must be updated. Using a text editor (`vim` or `nano`), set `HOSTNAME` to `{{NEW_HOSTNAME}}`, `LISTEN_PORT` to `{{LISTEN_PORT}}`, and `TIMEOUT` to `{{TIMEOUT_VAL}}`, and change `DEBUG=true` to `DEBUG=false`.
