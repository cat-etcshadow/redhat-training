## Queue a one-time job with batch

Your task:

1. The `atd` daemon must be running and enabled.
2. Use `batch` to queue a one-time job that writes the string
   **{{BATCH_MESSAGE}}** into **{{BATCH_OUTFILE}}**, to run as soon as the
   system load permits.
