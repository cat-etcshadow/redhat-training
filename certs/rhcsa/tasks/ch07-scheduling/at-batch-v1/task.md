## Queue a one-time job with batch

With the `atd` daemon running and enabled, use `batch` to queue a one-time job that writes the string **{{BATCH_MESSAGE}}** into **{{BATCH_OUTFILE}}**, to run as soon as the system load permits.
