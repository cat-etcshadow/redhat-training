## Restore a scheduled backup job

The script /usr/local/bin/rhtr-backup.sh is supposed to run automatically every night as the user **backupsvc** and create a new archive under /var/backups/app-data/, but no new archives have appeared in several days. Ensure the backup job runs automatically on its intended schedule going forward.
