## Manage the at queue: list and remove a specific job

Three one-time jobs are currently queued with `at`.

Your task:

1. Use `atq` to list the pending jobs.

2. Identify the job that would write to **{{REMOVE_FILE}}**, and remove it
   with `atrm`.

3. The other two queued jobs (the ones writing to **{{KEEP_FILE1}}** and
   **{{KEEP_FILE2}}**) must remain queued.
