## Adjust scheduling priority for all of a user's processes

The user **{{TARGET_USER}}** (already exists) owns several running background processes. Using a single command that targets the user rather than individual PIDs, change the nice value of every process owned by **{{TARGET_USER}}** to **{{NICE_VAL}}**.
