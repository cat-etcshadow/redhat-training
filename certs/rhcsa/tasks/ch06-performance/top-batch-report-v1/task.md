## Capture a process snapshot with top in batch mode

A CPU-intensive background process named **rhtr_cpu_hog** is running on
the system.

Your task:

Using `top` in **batch mode** (non-interactive, a single iteration),
capture a snapshot of current process activity and save it to
**{{OUTPUT_FILE}}**. The snapshot must include the **rhtr_cpu_hog**
process.
