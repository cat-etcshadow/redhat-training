## Capture a non-interactive process snapshot

A CPU-intensive background process named **rhtr_cpu_hog** is running on
the system.

Your task:

Capture a single, non-interactive snapshot of current process activity
and save it to **{{OUTPUT_FILE}}**. The snapshot must include the
**rhtr_cpu_hog** process.
