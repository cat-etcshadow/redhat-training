## Ensure a containerized application starts automatically at boot

The container **rhtr-webapp** is running correctly right now via its systemd service, but after a reboot it does not come back up on its own. Ensure **rhtr-webapp** starts automatically every time this system boots.
