## Manage a container's lifecycle: stop, and remove

Two containers are running: **rhtr-lifecycle-a** and **rhtr-lifecycle-b**. Stop rhtr-lifecycle-a without removing it, and remove rhtr-lifecycle-b entirely so it no longer appears even among stopped containers.
