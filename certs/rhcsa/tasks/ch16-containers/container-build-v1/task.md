## Build a container image from a Containerfile

A build directory has been created at **{{BUILD_DIR}}** containing a `Containerfile`. Build the image using `podman build`, tagging it as **{{IMAGE_TAG}}**, and confirm it works by running a container from it — it must print the expected message and exit cleanly.
