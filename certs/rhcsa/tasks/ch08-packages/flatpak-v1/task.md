## Configure Flatpak Repository Access and Manage Applications

The system does not currently have any Flatpak remotes configured.

Your task:

1. Ensure the `flatpak` package is installed.
2. Add the Flathub repository as a system-wide remote named `flathub`, using
   the repo file at `https://dl.flathub.org/repo/flathub.flatpakrepo`.
3. Install the application `org.gnome.gedit` from the `flathub` remote,
   system-wide.
4. Remove the application `org.gnome.Calculator`, which is already installed.
