## Enable a DNF module stream and install from it

Enable the **nodejs:{{NODE_STREAM}}** module stream and install the `nodejs` package from it. The installed `node` version must match the **{{NODE_STREAM}}** stream, and the stream must show as enabled.
