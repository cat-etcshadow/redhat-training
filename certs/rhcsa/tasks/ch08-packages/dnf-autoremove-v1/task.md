## Remove an orphaned dependency package

The package **{{PKG}}** is installed on the system, marked as having been installed automatically as a dependency. Nothing on the system depends on it anymore. Remove **{{PKG}}** from the system.
