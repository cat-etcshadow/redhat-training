## Restore package installation on this system

Attempting to install any package with `dnf` on this system fails, even for packages that are normally available from the standard configured repositories. Fix the underlying problem so that packages can be installed normally again.
