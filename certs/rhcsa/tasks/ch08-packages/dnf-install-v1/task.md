## Install a package and verify installation

Your task:

1. Install the **tmux** package using `dnf`.
2. Verify that the package is installed using `rpm -q tmux`.
3. Install the **man-pages** package.

Both packages must be installed and queryable with `rpm -q` after completion.
