## Restore a modified package file with dnf reinstall

The configuration file `/etc/httpd/conf/httpd.conf`, which is owned by the
**httpd** package, was accidentally modified.

Your task:

Use `dnf` to restore all files owned by the **httpd** package to their
original, package-provided state — without manually removing and
reinstalling the package by hand.
