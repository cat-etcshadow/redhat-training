## Restore a modified package file to its original state

The configuration file `/etc/httpd/conf/httpd.conf` was accidentally modified. Restore all files owned by the package that provides this file to their original, package-provided state.
