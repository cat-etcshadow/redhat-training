## Find and install the package that provides a missing command

A script on this system needs the command **{{BIN_PATH}}**, but it is not
currently installed anywhere on the system.

Your task:

1. Determine which package provides **{{BIN_PATH}}**.
2. Install that package.
3. **{{BIN_PATH}}** must exist and be executable afterward.
