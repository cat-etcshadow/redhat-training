## Find and install the package that provides a missing command

A script on this system needs the command **{{BIN_PATH}}**, but it is not currently installed anywhere on the system. Install the package that provides **{{BIN_PATH}}**, so that it exists and is executable afterward.
