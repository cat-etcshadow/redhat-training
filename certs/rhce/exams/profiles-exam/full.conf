NAME="Full RHCE — exam format (outcome-graded)"
DURATION=180
PASS_THRESHOLD=70
# Grows as more tasks-exam/ scenarios are curated (target ~15-20 per TODO.md
# Phase 12a) — capped at 1 for now since webserver-stack-v1 is the only one.
SCENARIO_COUNT=1
