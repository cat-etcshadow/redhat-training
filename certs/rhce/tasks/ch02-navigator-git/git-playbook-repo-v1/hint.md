## Hint

- `git clone {{BARE_REPO}} {{CLONE_DIR}}`
- `git add`, `git commit -m "message"`, `git push origin main` (or
  `HEAD:main` if your local branch is named differently)
- Set `user.name`/`user.email` with `git config` if commit fails with an
  identity error
